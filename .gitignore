# Rust build artifacts
/target/
**/*.rs.bk

# Node / pnpm
node_modules/
.pnpm-store/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS & Editors
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Golden-image failure artefacts. `graphics::golden` writes these next to the
# reference so a reviewer can see what was produced and which pixels moved; they
# are diagnostics, never inputs.
*.actual.png
*.diff.png
