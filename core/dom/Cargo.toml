[package]
name = "dom"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish.workspace = true
description = "DOM tree aggregate: an arena-backed DomTree with enforced tree invariants, validated value objects, non-recursive traversal, and deterministic HTML serialization. Zero dependencies; names no engine type (v0.2 F3, ADR-0003, ADR-0010)."

# Intentionally empty. `core/dom` is a pure domain crate (v0.2 report decision
# 2.1): no `engine`, no interpreter, nothing transitive. The DOM↔engine bridge
# lives in `core/runtime/rhai/infrastructure/` (roadmap I1).
[dependencies]

[lints]
workspace = true
