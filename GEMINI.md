# GEMINI.md

This repository keeps a single AI-agent guidance file to avoid two instruction sets drifting apart: **`CLAUDE.md`**.

Read it in full before making any change here. It applies identically regardless of which agent you are — crate map,
layering rules, Object Calisthenics, the `just gate` workflow, and (most importantly) the "Recurring Review Feedback"
section documenting mistakes already flagged more than once across past PRs. Do not duplicate its content into this
file; update `CLAUDE.md` instead and let this pointer stay a pointer.
